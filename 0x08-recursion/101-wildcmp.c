#include "main.h"

/**
 * wildcmp - Converting fanction
 * @s1: input string
 * @s2: input string
 * Return: Always Succes (0)
 */
int wildcmp(char *s1, char *s2)
{
	int result;

	result = wildcmp(char *s1,char *s2);
 	if (char *s1 == char *s2)
		return (1);
	else
		return (0);
}

