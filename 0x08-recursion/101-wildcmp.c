#include "main.h"

/**
 * wildcmp - Converting fanction
 * @s1: input string
 * @s2: input string
 * Return: Always Succes (0)
 */
int wildcmp(char *s1, char *s2)
{
	int result;

	ressult = wildcmp(*s1, *s2);
	if (*s1 == *s2)
		return (1);
	else
		return (0);
}

