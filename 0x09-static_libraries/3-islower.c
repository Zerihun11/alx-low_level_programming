#include "main.h"

/**
 * _islower - checking character if it's a lowercae character
 *  @c: The character to check
 * return: return 1 for lower char
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
