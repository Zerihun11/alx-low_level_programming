#include "main.h"

/**
 * _abs - computes the absolut values of an input
 * @n: the int to check
 * Return: The absolute values of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
