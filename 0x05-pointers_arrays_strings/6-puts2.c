#include "main.h"

/**
 * puts2 - Prints function
 * @str: String characters.
 */
void puts2(char *str)
{
	int lengi = 0, x = 0;
	char *y = str[lengi];
	int o;

	while (*y != '\0')
	{
		y++;
		leni++;
	{

	for (x = 0; x < lengi; x += 2)
	{
		{
			_putchar(str[x]);
		}
	}
	_putchar('\n');
}
