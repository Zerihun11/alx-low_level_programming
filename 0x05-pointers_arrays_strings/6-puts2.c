#include "main.h"

/**
 * puts2 - Prints function
 * @str: String characters.
 */
void puts2(char *str)
{
	int lengi, x;
	
	lengi = 0;
	*y = str[lengi]

	while (*y != '\0')
	{
		y++;
		leni++;
	{

	for (x = 0; x < lengi; x += 2)
	{
		{
			_putchar(str[x]);
		}
	}
	_putchar('\n');
}
